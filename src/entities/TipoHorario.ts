export class TipoHorario {
    id: number;
    descricao: string;
    situacao: string;
  
    constructor(
      id: number,
      descricao: string;
      situacao: string;
    ) {
      this.id = id;
      this.descricao = descricao;
      this.situacao = situacao;
    }
  }